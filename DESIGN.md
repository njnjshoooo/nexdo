# 好齡居 (HaoLingJu) - Design System

這份 `DESIGN.md` 定義了「好齡居 - 樂齡居住安全專家」官網的專屬設計規範。AI 代理或開發者在建立新元件或頁面時，請嚴格遵守此文件中的 9 大設計要素。

## 1. 整體氛圍 (Vibe & Tone)
- **溫暖、安心、專業、無障礙。**
- 專為「樂齡」與「長照」族群設計，避免過於冷硬的科技感或純白色的醫院冷漠感。
- 強調居家舒適與值得信賴的感覺，帶有大地與自然元素的沈穩感，視覺傳達出「把家交給我們很放心」。

## 2. 色彩配置 (Color Palette)
- **主色 (Primary)**: `#885200` (溫暖琥珀棕，用於主要按鈕與強調視覺)
- **主色淺 (Primary Light)**: `#B36B00` (用於 Hover 狀態或次要點綴)
- **輔助色 (Secondary)**: `#516438` (自然大地綠，用於次要按鈕、安全與成功提示)
- **輔助色淺 (Secondary Light)**: `#7A8F5C`
- **暖色背景 (Warm Background)**: `#FEFDF9` (米白底色，取代純白以降低長者視覺疲勞)
- **暖灰背景 (Warm Gray)**: `#F5F5F0` (用於卡片背景、區塊分割與大面積留白)
- **文字 (Text)**:
  - 標題文字 (H1-H3): `#1C1917` (高對比深灰，確保極佳易讀性)
  - 內文與描述 (Body): `#44403C` (清晰柔和的次要字色)
  - 備註與日期 (Caption): `#78716C` (輔助說明，勿使用過淺的灰色)

## 3. 字體排版 (Typography)
- **字體**:
  - 中文: `Noto Sans TC`, `sans-serif` (預設黑體，確保各裝置閱讀清晰)
  - 英文 / 數字: `Inter`, `sans-serif`
- **內文行高 (Line Height)**: `1.6` 到 `1.8` (為長者提供更大的閱讀呼吸空間)
- **段落間距 (Paragraph Spacing)**: `1.5em`
- **尺寸規範 (Accessibility First - 字體較大)**:
  - `H1` (首頁/主頁標題): 40px / Bold (700)
  - `H2` (區塊標題): 32px / Bold (700)
  - `H3` (卡片標題): 24px / SemiBold (600)
  - `Body` (一般內文): 18px (預設 1rem 基準放大，不低於 16px)
  - `Caption` (輔助文字): 16px (避免過小導致長者閱讀困難)

## 4. 間距規則 (Spacing)
- **xs (4px)**: 元素微調間距
- **sm (8px)**: Icon 與文字之間、標籤內部 Padding
- **md (16px)**: 內文段落間距、卡片內部緊湊資訊間距
- **lg (24px)**: 區塊內獨立元件之間 (如：按鈕與輸入框、卡片標題與內文)
- **xl (32px)**: 卡片整體 Padding 或中型區塊間距
- **2xl (64px)**: 用於首頁或長網頁中，不同大區塊 (Section) 之間的留白呼吸空間

## 5. 元件樣式 (Component Styles)
- **按鈕 (Button)**:
  - 形狀: `border-radius: 9999px` (全圓角 Pill 形狀，帶來親和感) 或 `12px` 圓角
  - Padding: 上下 `12px` / 左右 `24px` (確保長者點擊範圍夠大，高度至少 44px)
  - 樣式: 無邊框 (Border: none)，主按鈕使用 Primary 填色與白色文字
  - Hover 狀態: 顏色轉為 Primary Light，或降低透明度 (`opacity-90`)
- **卡片 (Card)**:
  - 形狀: `border-radius: 16px` (柔和的圓角)
  - 背景色: `#FFFFFF` (放在 `#F5F5F0` 背景上) 或 `#F5F5F0` (放在 `#FEFDF9` 背景上)
  - 邊框: 視情況加入 1px `#E7E5E4` 極細邊框增加立體輪廓

## 6. 陰影層次 (Shadows)
陰影不宜過重，以乾淨、輕盈為主，輔助長者辨識卡片與背景的圖層關係。
- **預設卡片 (Card)**: 極輕微的漫射陰影 `box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.05)`
- **浮動元件 (Dropdown, Modal)**: 較明顯的陰影 `box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1)`
- **Hover 狀態**: 微微浮起 `box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.08)`

## 7. 互動效果 (Interactive)
- **動畫速度**: `transition-all duration-300 ease-in-out` (平滑不過快，避免閃爍或暈眩感)
- **Hover 反饋**: 微幅向上浮動 (`-translate-y-1`) 與陰影加深，讓可點擊元素具有非常明確的反饋。
- **Focus 狀態**: 輸入框與按鈕在 Focus 時，必須有明顯的 Focus Ring (`ring-2 ring-primary ring-offset-2`)，支援無障礙鍵盤操作。

## 8. Do's and Don'ts
- **✅ DO (可以做):**
  - 確保所有文字與背景的對比度達到 WCAG AA 標準 (至少 4.5:1)。
  - 互動元素 (按鈕、連結、選單) 的點擊範圍必須夠大 (至少 44x44px)。
  - 表單輸入框要有清晰的 Label (標籤) 與 Placeholder，並加粗重要提示。
  - 大量使用空間留白 (Whitespace)，讓畫面不擁擠。
- **❌ DON'T (不要做):**
  - **絕對不要**使用淺灰色字體 (`#9CA3AF`) 在白底上，長者會看不見。
  - 避免過度複雜、閃爍的 CSS 動畫或進場效果。
  - 不要把重要資訊藏在需要「滑鼠懸停 (Hover)」才看得見的 Tooltip 裡，手機與平板無法操作，且對長者極不友善。

## 9. Prompt 範例 (Prompts)
如果你是 AI 代理，下次可以這樣被提示以生成符合本站風格的元件：
- **卡片生成:** 「請根據 DESIGN.md 的規範，幫我生成一個『居家防滑地墊』的商品展示卡片。需要包含大尺寸圖片、高對比的 24px 卡片標題，以及一個點擊範圍足夠大的 Primary Color (琥珀棕) 購買按鈕。」
- **區塊生成:** 「請幫我設計一個『聯絡我們』的區塊。背景使用 DESIGN.md 規範的暖灰背景色，標題文字使用 32px 深灰色，並確保所有表單輸入框的字體為 18px，以符合樂齡無障礙閱讀需求。」
