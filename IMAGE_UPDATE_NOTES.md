# 官網制服與整理服務更新

## 現行人物服裝原則

依使用者最新回饋，客戶與家屬穿自然便服；好齡居顧問、教練、搬運、清潔及醫療服務人員保留奶油白／深綠品牌制服。不能只用原衣服顏色判定角色：家庭合照中的綠衣家人仍改便服，醫療或諮詢場景中的白衣工作人員則保留制服。

本次重新檢視先前 106 張人物圖片，80 張有客戶／家屬的情境已完成便服調整。便服使用低飽和米色、灰藍、暖灰、豆沙等色系，運動場景採舒適運動上衣；客戶服裝前後皆移除好齡居標誌。熟齡受眾維持約 55–70 歲、打理過的深色或部分灰髮。

使用內建 imagegen 編輯。維持人物、姿勢、空間、醫療防護用品，以及安全檢測照片的濕滑地面、門檻、雜物與其他題目線索。

新資產保存於 `public/images/customers/`；逐張角色判定、完整提示與輸出檔案見 `docs/customer-clothing-edits.json`。原版制服資產保留於 `public/images/uniforms/`，先前提示記錄保留於 `docs/uniform-image-edits.json` 作為歷程。

`src/data/brandImageOverrides.json` 同時處理原始 CMS 網址及舊快取內的制服圖片路徑，讓首頁、服務頁、產品、文章與內嵌 HTML 使用新版便服照片。minigame 獨立 bundle 同步替換並更新檔名，避免舊圖片快取。

## 整理服務資料庫

`202609140002_unified_organizing.sql` 已在 Supabase 執行並透過公開讀取驗證：

- `organizing` 產品與服務頁已新增並公開。
- 三個舊階段頁面停止公開，保留產品與歷史紀錄。
- 三個舊整理產品的報價說明統一為「依整理範圍評估報價」。
- 空屋整理、清運的相關服務指向 `organizing`。
- 固定價格商品與其他商品內容比對未變更。
- 產品保留可公開讀取的來源圖片 URL，新版前台自動使用依人物角色調整的新版圖片，避免部署前出現破圖。

舊整理網址由前台導向單一「老前整理」，並預選目標；兩題選擇會進入可複製需求摘要。

## 驗證

圖片逐批目視檢查，修正背面圖案位置；主頁八項服務、整理目標／目的與摘要、舊網址轉向、兩個 minigame 的首頁連結及初始互動均已檢查。最新 TypeScript 與 production build 通過；80 張便服圖片、186 組原始／快取圖片映射及 14 張 minigame 客戶圖片路徑檢查通過。建置僅有既有的大型 bundle 與混合 import 提示。

前端更新由 PR 合併部署後上線；資料庫遷移已完成，不需要再次手動新增整理產品。
